{
  "name": "backend",
  "version": "1.0.0",
  "description": "Dockerized fullstack backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test",
    "test:regression": "node scripts/regression.js",
    "test:performance": "node scripts/performance.js",
    "test:summary": "npm run test:regression && npm run test:performance && node scripts/generate-test-summary.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "pg": "^8.16.3",
    "pg-mem": "^3.0.13"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
